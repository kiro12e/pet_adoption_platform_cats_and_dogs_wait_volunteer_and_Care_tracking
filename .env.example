# Copy this file to .env and fill values for your local environment
# Do NOT commit your real .env with secrets to version control.

# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=123456
DB_NAME=stray_cats_dog_adoption
DB_CONNECTION_LIMIT=10

# App configuration
PORT=3000
CORS_ORIGIN=http://localhost:5500
